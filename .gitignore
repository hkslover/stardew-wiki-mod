bin/
obj/
.dotnet-cli-home/
.nuget-packages/
.DS_Store
config.json

# ASR model for voice input is vendored in the repo (~25MB) so a clone builds without a
# separate download. Only model.int8.onnx + tokens.txt are tracked; ignore the rest
# (source archive, unused bbpe vocab, and sample wavs).
models/*.tar.bz2
models/**/bbpe.model
models/**/test_wavs/
